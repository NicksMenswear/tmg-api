name: run-release-stg
run-name: ${{ inputs.semver }}

on:
  workflow_dispatch:
    inputs:
      semver:
        type: string
        description: semver
        required: true

concurrency:
  group: ${{ github.workflow }}-stg
  cancel-in-progress: false

jobs:
  validate-semver:
    runs-on: ubuntu-latest
    steps:
      - name: Validate semver
        run: |
          if [[ ! ${{ inputs.semver }} =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Invalid semver: ${{ inputs.semver }}"
            exit 1
          fi
      
      - name: Ensure tag does not exist
        run: |
          release=$(curl -s -o /dev/null -w "%{http_code}" https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ inputs.semver }})
          if [[ $release_code == "200" ]]; then
            echo "Tag ${{ inputs.semver }} already exists."
            exit 1
          fi

  ci:
    uses: ./.github/workflows/_unit_tests.yml
    needs: validate-semver
    with:
      ref: ${{ github.sha }}

  cd:
    uses: ./.github/workflows/_sls_deploy.yml
    needs: ci
    with:
      ref: ${{ github.sha }}
      stage: stg
      url: https://github.com/${{ github.repository }}/releases/tag/${{ inputs.semver }}
      notify: true
      release_semver: ${{ inputs.semver }}
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  e2e-group-1:
    uses: ./.github/workflows/_e2e_tests.yml
    needs: cd
    with:
      ref: ${{ inputs.semver }}
      stage: stg
      notify: true
      test_group: group-1
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  e2e-group-2:
    uses: ./.github/workflows/_e2e_tests.yml
    needs: cd
    with:
      ref: ${{ inputs.semver }}
      stage: stg
      notify: true
      test_group: group-2
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  e2e-group-3:
    uses: ./.github/workflows/_e2e_tests.yml
    needs: cd
    with:
      ref: ${{ inputs.semver }}
      stage: stg
      notify: true
      test_group: group-3
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  e2e-group-4:
    uses: ./.github/workflows/_e2e_tests.yml
    needs: cd
    with:
      ref: ${{ inputs.semver }}
      stage: stg
      notify: true
      test_group: group-4
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  e2e-group-5:
    uses: ./.github/workflows/_e2e_tests.yml
    needs: cd
    with:
      ref: ${{ inputs.semver }}
      stage: stg
      notify: true
      test_group: group-5
    secrets:
      DATADOG_API_KEY: ${{ secrets.DATADOG_API_KEY }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

  release-status:
    runs-on: ubuntu-latest
    needs:
      - e2e-group-1
      - e2e-group-2
      - e2e-group-3
      - e2e-group-4
      - e2e-group-5
    if: ${{ always() }}
    permissions:
      id-token: write
      statuses: write
    steps:
      - name: Set commit status as pending
        uses: myrotvorets/set-commit-status-action@v2.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          status: "${{ needs.e2e-group-1.result == 'success' && needs.e2e-group-2.result == 'success' && needs.e2e-group-3.result == 'success' && needs.e2e-group-4.result == 'success' && needs.e2e-group-5.result == 'success' && 'success' || 'failure' }}"
          context: "run-release-stg / e2e"
          sha: ${{ github.sha }}
          targetUrl: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
